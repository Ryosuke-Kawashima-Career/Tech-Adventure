[project]
name = "python-async-fetcher"
version = "0.1.0"
description = "Asynchronous LinkedIn profile fetcher using aiohttp"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9.0",
]

[tool.uv]
dev-dependencies = []